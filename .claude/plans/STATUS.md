# Project Status

> Updated: 2026-05-27 (avatax v1 closed: Phase 1-4 complete, Bug #1/#2/#3 fixed, void+adjust deferred to v2 by product decision)
> Rule: Claude must update this file at the end of every task.

| Module | Status | % | Last Touched | Notes |
|---|---|---|---|---|
| user | done | 100% | — | CRUD, roles, permissions, tests |
| permissions | done | 100% | — | Spatie, seeders, policy |
| department | done | 100% | — | CRUD, tests |
| customer | done | 100% | 2026-05-26 | CRUD, status, force-verify, password reset, tests. tax_exempt: migration + model + fillable + cast + UpdateCustomerRequest rule + edit form checkbox + 3 Pest tests (28/28 passing). |
| customer-address | done | 100% | 2026-05-22 | Admin + portal, auto-default, IDOR protection, tests |
| portal-foundation | done | 100% | 2026-05-21 | Auth, middleware, layout, tests |
| portal-profile | done | 100% | 2026-05-22 | Profile, password change, tests |
| mail | in progress | 85% | 2026-05-21 | Phases 1-3 done; Phase 4 (AccountDeactivatedMail) deferred |
| supplier | done | 100% | — | CRUD, tests |
| product | done | 100% | — | CRUD, tests |
| product-category | done | 100% | — | CRUD, tests |
| product-listing | done | 100% | — | CRUD, SEO, slug, tests |
| purchase-order | done | 100% | — | PO + GRN + Invoice, tests |
| inventory | done | 100% | — | Location, movement, serial, tests |
| audit-log | done | 100% | — | Controller, views |
| order | done | 100% | 2026-05-26 | **REBUILT from `.claude/plans/Orders/` (18-file strict-TDD plan, ex-19 fixture).** New schema: per-line `order_line_fees` table, dropped `order_fees`, dropped `tax_rate`, receipt-style math (`line_total + fee_total + shipping = grand_total`), shop-as-billing for cash, hard-delete with Spatie ActivityLog audit trail. **Fix #1:** shop config optional (null defaults, conditional letterhead, AvaTax ship_from guard). **Fix #2:** create.blade.php table layout with `data-testid="items-table"`. **Fix #3:** edit.blade.php rebuilt to mirror create's table layout + hydrates ALL state from `window.__existingOrder` (controller passes nested JSON of customer/source/payment/shipping/lines/fees) → fields no longer reset on edit render. **Fix #4:** show.blade.php "Record Cash Payment" replaced with modal (`data-testid="record-payment-modal"`) — shows amount due, editable amount received, cashier name, received-at timestamp, real form POSTs to `orders.cash-payment`. **Fix #5:** "+ New address" buttons + modal (`data-testid="new-address-modal"`) on create + edit forms — POST JSON to new endpoint `orders.customer-addresses.store` (new `OrderController::storeCustomerAddress` + `StoreCustomerAddressFromOrderRequest` + route). **Fix #7:** `OrderSeeder` creates Texas Test Buyer + Austin TX address for AvaTax verification. Also fixed Customer model `$fillable` + `$casts` for `tax_exempt` (was missing). Plan files 07/08/10/11/12/13/15/18/00 updated. 62/62 order+AvaTax tests passing. **Fix #6:** for cash walk-in, payment IS the sale event. `recordCashPayment()` now does: (1) allocate serial per line (moved from `store()`), (2) `InventoryMovementService::recordSale()` to create the `inventory_movements` row + flip serial to `Sold` (moved from `complete()`), (3) create payment row, (4) `pending → processing`. `complete()` retained as a pure status finalizer (`processing → complete`) — no inventory work. **Fix #A (2026-05-27):** `resolveBillingSnapshot` now honors customer-chosen `billing_address_id` even for cash payment (previously always overrode with shop billing → "Not provided" bug). **Fix #B:** `OrderController::edit` matches snapshot columns back to a CustomerAddress so dropdowns pre-select on edit (no schema change, opportunistic match). **Fix #C:** `OrderSeeder` registered in `DatabaseSeeder` so `migrate:fresh --seed` creates Texas Test Buyer for AvaTax end-to-end verification. **Fix #D:** receipt extracted into `ReceiptService` (assembles shop/order/customer/lines/totals/payments/footer arrays) + `ReceiptController` (thin authorize+render). View consumes `$receipt` data structure → header/footer overrides centralized. Plan files 07/14/15/11 + view + routes updated. **Security pass (2026-05-27):** addressed all CRITICAL + HIGH findings from `/code-review` + `/security-review`: (CRIT-1) Cross-customer address IDOR closed — `Rule::exists(...)->where('customer_id', ...)` on both `StoreOrderRequest`/`UpdateOrderRequest` for `billing_address_id` + `shipping_address_id`; `StoreCustomerAddressFromOrderRequest::authorize()` now checks `CustomerAddress::create` ability scoped to the resolved Customer. (CRIT-2) `OrderController::calculateTax` now uses `CalculateTaxRequest` FormRequest (max 50 lines / 10 fees per line, all fields validated). (HIGH) `Order::$fillable` stripped of `status`, `payment_status`, `grand_total` — service uses `forceFill()` for server-set fields. (HIGH) `Payment::$fillable` stripped of `cash_received_at`, `created_by` — service `forceFill()`. (HIGH) `customerAddresses` endpoint adds `authorize('view', $customer)` to prevent PII enumeration. (HIGH) `edit()` eager-loads `payments` (was lazy-loaded). (HIGH) Snapshot-matcher now compares 5 fields (line1+city+state+postal+country) to reduce false-positive injection risk. **Seeder hygiene:** `OrderSeeder` guards `app()->environment(['local','testing'])` so the bcrypt('password') fixture never ships to production. **Plan files:** 09-requests.md + 11-controller.md updated to reflect security fixes. **MEDIUM batch (2026-05-27):** (#1) `RecordCashPaymentRequest` adds `max:99999999.99` ceiling on amount; (#2) `orders.calculate-tax` route adds `throttle:30,1` middleware (30 reqs/min/user) to prevent AvaTax quota burn; (#3) `07-service.md` private-helpers list cleaned up to match implementation (removed phantom `assignSerialsToLines`/`recordSaleMovements`, added `allocateSerial`); (#7) +3 calculateTax feature tests (disabled-AvaTax zeros, tax-exempt zeros, viewAny perm required). **Second MEDIUM batch (2026-05-27):** (#4) `OrderService::recordCashPayment` now uses `$locked` consistently after lockForUpdate — `$order` was correct for DB writes but model in-memory state went stale, a foot-gun for future code in the closure; (#5) explicit `$this->authorize()` calls added to `store`/`update`/`recordCashPayment`/`storeCustomerAddress`/`calculateTax` — belt-and-suspenders, consistent with the rest of the controller; (#6) `OrderPolicy::recordCashPayment` gains a 4-line comment documenting the intentional triplication (policy → 403 first, service → defense in depth + reusable, view → UX); (#8) `StoreCustomerAddressFromOrderRequest::rules()` `customer_id` rule now uses `Rule::exists('customers','id')->whereNull('deleted_at')` so soft-deleted customers produce a clean 422 (not 404). +1 feature test confirms rejection. 87/87 order+AvaTax+receipt tests passing. |
| payment | not started | 0% | — | Depends on order |
| shipment | not started | 0% | — | Depends on order |
| refund | not started | 0% | — | Depends on order |
| replacement | not started | 0% | — | Depends on order |
| complaint | not started | 0% | — | Schema plan only |
| avatax | done | 100% (v1) | 2026-05-27 | Phase 1 done. Phase 2 done: AvaTaxService::calculateTax(), POST /admin/orders/calculate-tax, tax_exempt on Customer, Alpine fetchAllLineTax() debounce on create+edit forms, OrderService uses submitted tax_amount. Both forms: editable tax_amount input (admin can override AvaTax), edit form rewritten as table matching create (Product/SKU/Stock/UnitPrice/Tax$/Subtotal), onProductChange+loadStock on edit. **Phase 3 (2026-05-27):** AvaTaxService::commitInvoice() added — creates `SalesInvoice` with `commit=true` and `code=order_number` so AvaTax records it in their ledger for tax filing. OrderService::recordCashPayment() now calls commitInvoice() synchronously **after** the DB transaction commits (best-effort: failure logs but never rolls back the recorded payment). Lines + per-line fees both submitted. Ship-to prefers shipping snapshot, falls back to billing for pickup. `makeClient()` widened to `protected` for testability. **Tests:** 3 guard tests + 1 mocked happy-path test (asserts type=SalesInvoice, commit=true, code=order_number, full payload) + 1 OrderService wiring test (spy on AvaTaxService verifies commitInvoice called with 3 lines + correct customer/document codes). **Seeder:** AvaTaxTestSeeder adds 3 edge-case customers — Houston Local (same-city → store-local rate), California (cross-state rate), Tax-Exempt (verifies $0 path). Registered in DatabaseSeeder. **Plan files:** 03-order.md authored (RED→GREEN→REFACTOR + design notes + out-of-scope follow-ups: void on cancel, adjust on refund, retry queue). 88/88 AvaTax+order tests passing. Out of scope (blocking 100%): void on cancel, adjust on partial refund, retry on commit failure. |
| note | not started | 0% | — | Schema plan only |
| core-returns | in progress | 20% | 2026-05-22 | 01-schema.md plan created; 11 examples in system-design/examples/; migrations/models/services not started |

## % Guide

| % | Milestone |
|---|---|
| 20% | Schema + migration |
| 40% | Model + service |
| 60% | Controller + routes |
| 80% | Views |
| 90% | Tests written |
| 100% | All tests passing |
